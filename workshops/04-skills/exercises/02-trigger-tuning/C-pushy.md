---
name: bundle-size-check
description: |
  MANDATORY — invoke whenever user mentions bundle, weight, dependency
  size, dist size, build output, or any concern about JavaScript
  payload. Even subtle hints like "is it big" in a frontend context —
  trigger this skill. Use also for diagnosing webpack/vite output bloat,
  comparing builds, or evaluating dependency cost.
allowed-tools: [Bash]
---
