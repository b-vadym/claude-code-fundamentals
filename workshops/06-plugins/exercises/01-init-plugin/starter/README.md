# Starter — вправа 1

Структура створена для тебе. Залишилось:

1. Заповнити `git-toolkit/.claude-plugin/plugin.json` (поки порожній)
2. Запустити `claude plugin validate ./git-toolkit`
3. Запустити `claude --plugin-dir ./git-toolkit` і перевірити `/plugin`

Дивись `../README.md` для деталей.
