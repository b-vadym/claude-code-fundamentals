# Starter — вправа 3

`git-toolkit/` з вправи 2 скопійовано (skill + command + agent).

Додай:

- `git-toolkit/hooks/hooks.json` — `PostToolUse` matcher на `Write|Edit`
- `git-toolkit/scripts/log-edit.sh` — bash-скрипт з `${CLAUDE_PLUGIN_DATA}/edits.log`
- `chmod +x` на скрипт

Дивись `../README.md` для змісту файлів.
