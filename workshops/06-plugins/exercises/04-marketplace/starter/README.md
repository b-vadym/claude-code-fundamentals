# Starter — вправа 4

`git-toolkit/` з вправи 3 скопійовано (skill + command + agent + hook).

Тобі залишилось:

1. Створити `vcdev-marketplace/.claude-plugin/marketplace.json`
2. Перенести `git-toolkit/` у `vcdev-marketplace/plugins/`
3. Зробити install flow через `/plugin marketplace add` + `/plugin install`

Дивись `../README.md` для змісту файлів.
