# Starter

Скопіюй сюди фінальний `server.py` + `notes.md` з вправи 3 (або з `solutions/03-add-prompt/`).

```bash
cp ../../solutions/03-add-prompt/server.py .
cp ../../solutions/03-add-prompt/notes.md .
```

Далі — крок 2 у `../README.md` (`claude mcp add`).
